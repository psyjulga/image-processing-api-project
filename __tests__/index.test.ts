// import * as sharp from 'sharp'

// console.log(sharp)

// import app from '../src/index'

it('example test', () => {
    const value = 3
    expect(value).toBe(3)
})

test('whatever', () => {
    expect(5).not.toBe(10)
})

test('server is running', () => {
    // ??
})
